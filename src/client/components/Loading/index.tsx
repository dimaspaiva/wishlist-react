import React from "react";

import { LoadingContainer } from "./style";

const Loading = () => {
  return (
    <LoadingContainer>
      <p>Carregando...</p>
    </LoadingContainer>
  );
};

export default Loading;
